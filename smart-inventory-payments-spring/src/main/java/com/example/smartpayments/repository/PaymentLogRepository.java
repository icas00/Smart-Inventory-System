package com.example.smartpayments.repository;

import org.springframework.stereotype.Repository;

@Repository
public interface PaymentLogRepository {
    // Extend JPA repo later if you add DB logging
}
